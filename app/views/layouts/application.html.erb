<!DOCTYPE html>
<html>
<head>
  <title>VotionAppNew</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body style="text-align:center">
  <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
  <% end %>
  
  
  <header>
     <% if !current_user.blank?&&current_user.admin == true %>
      <ul>
        <li class="header-left">管理者メニュー</li>
        <li class="header-right"><%= link_to "投資者一覧", users_path %></li>
        <li class="header-right"><%= link_to "投資先一覧", candidates_path %></li>
        <li class="header-right"><%= link_to "管理者一覧", admin_users_path %></li>
      </ul>
    <% end %>
  </header><br>
  

  <%= yield %>

</body>
</html>
